# haleluya
# kerja di branch masing masing
# kalau tiap mau kerja git pull origin main dulu
# kalau mau push ke branch pribadi 
# kalau mau buat branch pakai git checkout -b nama-branch
# git add .
# git commit -m "Pesan anda"
# git push origin (tergantung nama branch)
